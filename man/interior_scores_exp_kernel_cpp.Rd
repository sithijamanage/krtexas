% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{interior_scores_exp_kernel_cpp}
\alias{interior_scores_exp_kernel_cpp}
\title{Compute interiorness scores using the exp kernel}
\usage{
interior_scores_exp_kernel_cpp(X, gamma, exclude_self = TRUE, normalize = TRUE)
}
\arguments{
\item{X}{Numeric matrix (n x p): sample points.}

\item{gamma}{Numeric vector (length p): nonnegative kernel scales per dimension.}

\item{exclude_self}{logical: if TRUE, set K(X_i, X_i) = 0 in the sum.}

\item{normalize}{logical: if TRUE, divide by n (number of points).}
}
\value{
Numeric vector (length n): interiorness scores (larger = more interior).
}
\description{
For each sample point X\link{i,}, compute
score\link{i} = (1/n) * sum_{k=1}^n exp( - sum_{j=1}^p gamma_j * (X\link{i,j} - X\link{k,j})^2 )
Optionally exclude self-similarity which only shifts all scores by a constant 1.
}
\examples{
\dontrun{
scores <- interior_scores_exp_kernel_cpp(X, gamma)
order <- order(scores, decreasing = TRUE)
}
}
