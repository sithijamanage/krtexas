% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/krtexas_functions.R
\name{krtexas_fit}
\alias{krtexas_fit}
\title{Fit KR TEXAS model (adaptive-penalty version)}
\usage{
krtexas_fit(
  X,
  Y,
  A,
  kernel = "gaussian",
  nfolds = 4,
  lambda = NULL,
  min_lambda = 0,
  max_lambda = NULL,
  nlambda = 10,
  alpha = 0,
  eps = 1e-06,
  gamma_threshold = 0,
  use_num_grad = FALSE,
  silent = TRUE,
  parallel = TRUE,
  n_cores = NULL,
  warm_start = TRUE,
  method = "LLR",
  gamma_init_strat = "smallest",
  distance = "L2",
  num_restarts_stage_1 = 10,
  num_restarts_stage_2 = 10,
  max_attempts_stage_3 = 10,
  developer_code = 0
)
}
\arguments{
\item{X}{Numeric matrix of predictors (n x p).}

\item{Y}{Numeric response vector of length n.}

\item{A}{Numeric matrix defining the tree / aggregation structure (M x p).}

\item{kernel}{Kernel type, "gaussian" or "epanechnikov".}

\item{nfolds}{Number of CV folds.}

\item{lambda}{Optional fixed lambda. If NULL, a lambda path is considered.}

\item{min_lambda}{Minimum lambda for path (if lambda NULL).}

\item{max_lambda}{Maximum lambda for path (if lambda NULL).}

\item{nlambda}{Number of lambdas in path.}

\item{alpha}{Sigma penalty parameter.}

\item{eps}{Optimizer tolerance (factr for L-BFGS-B).}

\item{gamma_threshold}{Threshold for zeroing gammas.}

\item{use_num_grad}{Whether to force numerical gradient (unused in current code).}

\item{silent}{If TRUE, suppresses most output from optim.}

\item{parallel}{Whether to use parallel CV.}

\item{n_cores}{Number of cores for parallel CV (default: detectCores() - 1).}

\item{warm_start}{Whether to warm-start gammas across lambdas.}
}
\value{
A list with elements:
\itemize{
\item gammas_learned
\item lambda.best
\item sigma_opt
\item training_loss
\item cv_lambda_losses
\item convergence
\item inputs (X, Y, A, kernel)
}
}
\description{
This is the high-level user-facing function. It first fits KR TEXAS with
lambda = 0 to obtain initial weights, then re-fits with an adaptive penalty.
}
